import { ConnectwiseCompany } from './connectwiseCompany.model';

describe('DattoDomain', () => {
  it('should create an instance', () => {
    expect(new ConnectwiseCompany()).toBeTruthy();
  });
});
